# Workflow Blocker 汇总报告
生成时间: {timestamp}
分析范围: _index.jsonl 中 {N} 个任务，{M} 个有 Blocker，共 {K} 条记录

---

## 按类型聚合

### 环境问题（共 N 次）
| 频次 | 场景 | 根因 | 建议 | 对应文件 |
|------|------|------|------|---------|
| 3次 | mvn compile 失败 | 依赖缺失 | 在骨架生成阶段增加依赖检查 | generator.md §X |

### 执行问题（共 N 次）
...

### 信息问题（共 N 次）
...

### 流程设计缺陷（共 N 次）
...

---

## P0 / P1 / P2 改进建议

### P0：阻断性问题

1. **[{N} 次] {问题描述}**
   - 根因：{根因}
   - 影响：{影响了多少任务}
   - 建议：{具体可操作的方案}
   - 对应文件：{建议修改的文件}

### P1：严重影响

...

### P2：优化项

...

---

## Evaluator Verdict 度量

> 数据源：`docs/reports/metrics/eval-verdicts.jsonl`（evaluator 每次跑追加一行）
> 缺失数据源 → 整段标"暂无数据,evaluator 尚未跑过"

### Phase 失败分布

| 阶段 | FAIL 次数 | ERROR 次数 | 占总失败比 |
|------|----------|-----------|----------|
| Phase 1 (code review) | 12 | 0 | 30% |
| Phase 2 (integration test) | 28 | 3 | 70% ⚠️ |

**解读**：哪个阶段拦得最多 → 反映团队工程能力的薄弱环节
- Phase 1 占比高 → 代码规范 / 复用 / 命名 是主要问题
- Phase 2 占比高 → 实现逻辑 / 契约理解 是主要问题

### AC 失败热点（top 5）

| AC ID | 关联 story | 失败次数 | 最常见 reason |
|-------|----------|---------|-------------|
| AC-003 | 05-20-sf-token-retry | 5 | expected 401 but got 500 |
| AC-007 | 05-22-rate-limit | 4 | 重试间隔未按指数退避 |
| ... | ... | ... | ... |

**解读**：反复失败的 AC → contract / spec 描述可能模糊 → 反哺 doc-librarian 或 planner 改进

### Retry 分布

| 指标 | 数值 |
|------|------|
| 平均 retry 次数 | 1.4 |
| 0 retry（一次过）| 18 任务 / 60% |
| 1-2 retry | 10 任务 / 33% |
| 3 retry 触顶 | 2 任务 / 7% ⚠️（需介入） |

**解读**：retry 触顶意味着 evaluator 反复挂 + Generator 反复改但仍 FAIL → 通常是 spec 模糊 / 规则误判 / 测试环境问题。**触顶任务应单独列出 root cause**。

#### 触顶任务列表
- {task_id}: {简述失败模式}

---

## 趋势分析（对比上次）

上次报告时间：{date}

| 维度 | 上次 | 本次 | 变化 |
|------|------|------|------|
| 环境-编译 blocker | 3次 | 1次 | ↓ 减少 |
| 信息-契约歧义 blocker | 1次 | 3次 | ↑ 增加 ⚠️ |
| 流程-顺序错误 blocker | 0次 | 1次 | ↑ 新增 |
| Phase 1 / Phase 2 失败占比 | 50% / 50% | 30% / 70% | Phase 2 上升 ⚠️ |
| 平均 retry 次数 | 1.1 | 1.4 | ↑ 上升 |
| AC-003 失败次数 | 1 | 5 | ↑ 显著恶化 ⚠️ |

---

## 未解决 Blocker（待跟进）

- 05-20-sf-token-retry-02: 信息-契约歧义（等 PM 确认）
- 05-20-sf-token-retry-03: 环境-编译（pom 依赖缺失）
