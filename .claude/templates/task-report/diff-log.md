# {task_id} 文件改动记录

> ⚠️ **由 file-tracker.py Hook 自动追加，Agent 不编辑本文件**。
> 记录所有 Edit / Write / Bash 操作，每条包含时间戳、文件路径、改动摘要。

---

## 使用说明

- **同文件多次改动**：每次都追加，展示改动演进
- **Bash 命令**：记录命令 + exit code（失败时标红）
- **diff 摘要**：`+ 新增` / `~ 修改` / `- 删除`，不超过 3 行摘要

---

## 改动记录

<!-- file-tracker.py 追加，格式如下 -->

## {timestamp}

**文件**: `{path}`
**工具**: `{Edit|Write|Bash}`
**操作**: `{+ 新增|~ 修改|- 删除}`

```diff
{简短 diff 摘要，不超过 3 行}
```

<!-- Bash 命令时 -->
**命令**: `mvn compile`
**Exit**: `0` / `1` ❌

---

<!-- 继续追加，Hook 负责维护此文件 -->
